version: '3'
services:
  node:
    build:
      context: .
    command: npm run test
    ports:
      - 80:3000
    environment:
      - DBHOST=mongo
      - DBPORT=27017
      - DATA=gateways-test
      - WAIT=5000
    depends_on:
      - mongo
  mongo:
    image: mongo
    restart: always
    ports:
      - 27017:27017
#    volumes:
#      - mongodb:/data/db
